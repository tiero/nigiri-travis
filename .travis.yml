sudo: true
services:
  - docker
 
before_install:
  - curl https://raw.githubusercontent.com/vulpemventures/nigiri-travis/master/index.sh | bash

install:
  - docker-compose up -d

script:
  - docker ps -a
  - curl http://localhost:3000/blocks/tip/height
  - curl http://localhost:3001/blocks/tip/height


after_script:
  - docker-compose down
